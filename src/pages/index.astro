---
import '../styles/global.css'
import Splash from '../components/Splash.astro'
import { getReleaseSummary, RELEASES_PAGE } from '../lib/release'

const { version } = await getReleaseSummary()

// JSON-LD structured data. Astro lets us define it as a regular object
// and stringify in the template — no separate `<Helmet>` ceremony, no
// gatsby-script. Just JSON.
const ldSoftware = {
    '@context': 'https://schema.org',
    '@type': 'SoftwareApplication',
    name: 'ROX.ONE Terminal',
    operatingSystem: ['macOS', 'Linux', 'Windows'],
    applicationCategory: 'DeveloperApplication',
    softwareVersion: version,
    url: 'https://rox.one',
    downloadUrl: RELEASES_PAGE,
    offers: { '@type': 'Offer', price: '0', priceCurrency: 'USD' },
}
const ldWebsite = {
    '@context': 'https://schema.org',
    '@type': 'WebSite',
    name: 'ROX.ONE',
    url: 'https://rox.one',
    inLanguage: ['ru', 'en'],
}
---

<!DOCTYPE html>
<html lang="ru" class="splash-locked">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
        <meta name="theme-color" content="#08090C" />
        <meta name="apple-mobile-web-app-capable" content="yes" />
        <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" />
        <meta name="apple-mobile-web-app-title" content="ROX.ONE" />
        <title>ROX.ONE</title>
        <meta name="description" content="Agent-native терминал для самых мощных LLM." />

        <meta property="og:title" content="ROX.ONE" />
        <meta property="og:description" content="Agent-native терминал для самых мощных LLM." />
        <meta property="og:type" content="website" />
        <meta property="og:url" content="https://rox.one/" />
        <meta property="og:image" content="https://rox.one/og/default.png" />
        <meta property="og:locale" content="ru_RU" />
        <meta property="og:locale:alternate" content="en_US" />
        <meta name="twitter:card" content="summary_large_image" />

        <link rel="canonical" href="https://rox.one/" />
        <link rel="alternate" hreflang="ru" href="https://rox.one/" />
        <link rel="alternate" hreflang="en" href="https://rox.one/en/" />
        <link rel="alternate" hreflang="x-default" href="https://rox.one/" />

        <link rel="icon" type="image/svg+xml" href="/icon.svg" />
        <link rel="apple-touch-icon" href="/apple-touch-icon.png" />
        <link rel="manifest" href="/manifest.webmanifest" />
        <link rel="alternate" type="application/rss+xml" title="ROX.ONE releases" href="/feed.xml" />

        <script is:inline type="application/ld+json" set:html={JSON.stringify(ldSoftware)} />
        <script is:inline type="application/ld+json" set:html={JSON.stringify(ldWebsite)} />
    </head>
    <body class="splash-locked">
        <a href="#main" class="skip-link">К содержимому</a>
        <main id="main" class="fixed inset-0 grid place-items-center overflow-hidden">
            <Splash locale="ru" />
        </main>
    </body>
</html>
